/*! Local placeholder for Three.js 0.160.0
 * This project runs without external CDN dependencies.
 * Replace with official three.min.js if full WebGL feature parity is required.
 */
(function initLocalThreePlaceholder(global) {
  "use strict";
  if (!global) {
    return;
  }
  if (!global.THREE) {
    global.THREE = {
      REVISION: "0.160.0-local-placeholder",
      SRGBColorSpace: "srgb",
      DoubleSide: 2,
      LinearFilter: 100,
    };
  }
})(window);
